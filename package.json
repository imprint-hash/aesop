{
  "name": "aesop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Aesop: collects and reinvests Fables liquidity fees on Robinhood Chain, executed by KeeperHub.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "status": "NODE_OPTIONS=--dns-result-order=ipv4first node bin/status.mjs",
    "claim": "NODE_OPTIONS=--dns-result-order=ipv4first node bin/claim.mjs",
    "compound": "NODE_OPTIONS=--dns-result-order=ipv4first node bin/compound.mjs",
    "rewards": "NODE_OPTIONS=--dns-result-order=ipv4first node bin/rewards.mjs"
  }
}
